// import 'package:dartz/dartz.dart';
// import 'package:flutter_test/flutter_test.dart';

void main() {
  // Todo: declare the variables

  // setUp(() {
  // Todo: Initial all variable declared
  // });

  /*
  group('FileNameType =>', () {
    // write tests
    test(
      'the function Name should return response type',
      () async {
        //!Arrange - Setup facts, Put Expected outputs or Initilize

        // late Either<String, int> testValue;
        // late int response;
        // try {
        //   response = 1 + 2;
        // } catch (e) {
        //   testValue = Left(Failures.message(Errors.calculator));
        // }
        // testValue = Right(response);

        //!Act - Call the function that is to be tested

        // final verifyValue =
        //     await _calculatorImpl.addition(numberOne: 1, numberTwo: 2);

        //!Assert - Compare the actual result and expected result
        
        // expect(verifyValue, testValue);
      },
    );
  });
  */
}
