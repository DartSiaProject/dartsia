// import 'package:calculator/src/injection.dart' as insert;
// import 'package:flutter_test/flutter_test.dart';

void main() async {
  // await insert.initialization();

  // Todo: declare the variables

  // setUp(() {
  // Todo: Initial all variable declared
  // });

/*
group('FileNameType =>', () {
    // write tests

    test(
        'the addition function should return good response or Error which is a string Type',
        () async {
      //Arrange - Setup facts, Put Expected outputs or Initilize
      final testValue =
          await _calculatorController.addition(numberOne: 1, numberTwo: 2);

      //Act - Call the function that is to be tested

      final verifyValue = await Calculator.addition(numberOne: 1, numberTwo: 2);

      //Assert - Compare the actual result and expected result
      expect(testValue, verifyValue);
    });
  });

  */
}
