// import 'package:dartz/dartz.dart';
// import 'package:flutter_test/flutter_test.dart';
// import 'package:mocktail/mocktail.dart';

// class MockSubject extends Mock implements Subject {}

void main() async {
  // Todo: declare the variables

  // setUp(() {
  // Todo: Initial all variable declared
  // });

/*
  group('FileNameType => ', () {
    // write tests

    test(
        ' the function Name should return response type',
        () async {
      //Arrange - Setup facts, Put Expected outputs or Initilize
      when(() => _mockCalculatorAbst.addition(numberOne: 1, numberTwo: 2))
          .thenAnswer((_) async => const Right(3));
      late String res;
      late Either<String, int> result;
      result = await _mockCalculatorAbst.addition(numberOne: 1, numberTwo: 2);
      result.fold(
        (fail) {
          res = "error";
        },
        (result) {
          res = "3";
        },
      );

      final verifyVariable =
          await _calculatorController.addition(numberOne: 1, numberTwo: 2);

      //Assert - Compare the actual result and expected result
      expect(verifyVariable, res);
      verify(() => _mockCalculatorAbst.addition(numberOne: 1, numberTwo: 2));
      verifyNoMoreInteractions(mock);
    });

  */
}
